# API Reference

## GET /health
Returns API health and Microsoft IQ branding.

## POST /api/incidents/analyze
Accepts `IncidentInput` and returns `AnalysisResult` containing normalized incident, findings, root causes, executive report, agent contributions, confidence, and IQ sources.
